import { ApiStatusObjectType } from "@/types/utilsType";

const IDLE = "IDLE";
const PENDING = "PENDING";
const ERROR = "ERROR";
const SUCCESS = "SUCCESS";

export const apiStatus: ApiStatusObjectType = {
  IDLE,
  PENDING,
  ERROR,
  SUCCESS,
};
